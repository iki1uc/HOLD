HOLD_OUTPUT.log();
const out = HOLD_OUTPUT.dump();

HOLD_SPLIT.state();
HOLD_SPLIT.freeze();
HOLD_SPLIT.pipe();

[GEO-HOLD]
ID → Freeze → State → Pipe → Result → Map → Field → Vector → Hub

<script type="module">
import { HOLD_OUTPUT } from "./HOLD.output.js";

HOLD_OUTPUT.log();
</script>

## HOLD
state  
freeze  
core  
pipe  
result  
output  
split  
geo

import { HOLD_constellation_output } from "./HOLD.output.js";
import { HOLD_constellation_split } from "./HOLD.split.js";

HOLD_constellation_output.log();
const out = HOLD_constellation_output.dump();

console.log(HOLD_constellation_split.state());
console.log(HOLD_constellation_split.freeze());
console.log(HOLD_constellation_split.pipe());
